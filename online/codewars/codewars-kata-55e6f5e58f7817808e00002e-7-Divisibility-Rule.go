package main

import "fmt"

func main(){

}

func Seven(n int64) []int64 {
	// convert to the form n = 10x + y
    // x -2y % 7 (subrtact twice the last digit from the number formed by the remaining digits)
    // keep doing until a number of 2 or less digits is found
	strN := str(n)
}